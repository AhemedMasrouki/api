import React from 'react'

const User.list = ({listofuser}) => {
  return (
    <div>
{listofuser.map(   user=><UserCard/>   user=(user>))}
    </div>
  )
}

exporser.list{listofusUr}{listuser=(userofuser.map(user=></userCard>)}